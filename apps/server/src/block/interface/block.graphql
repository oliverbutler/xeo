enum BlockType {
  PAGE
  TEXT
}

type Block {
  id: ID!
  type: BlockType!
  createdBy: User!
  createdById: ID!
  parent: Block
  parentId: ID
  children: [Block!]!
}

input CreateBlockInput {
  type: BlockType!
  parentId: ID
}

type Query {
  blocks: [Block!]!
}

type Mutation {
  createBlock(input: CreateBlockInput!): Block!
}
