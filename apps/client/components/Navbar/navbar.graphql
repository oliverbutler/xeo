query GetPath($id: ID!) {
  path(id: $id) {
    id
    ... on Page {
      properties {
        title {
          rawText
        }
        image {
          ... on Image {
            image
          }
          ... on Emoji {
            emoji
          }
        }
      }
    }
  }
}
