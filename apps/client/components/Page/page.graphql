query GetPage($id: ID!, $populateSubTree: Boolean!) {
  page(id: $id) {
    id
    emoji
    rawText
    richText
    favourite
    coverGradient
    blocks {
      id
      type
      richText
      rawText
      rank
      variant
    }
  }
}
