fragment PageBlock on Block {
  id
  type
  richText
  rawText
  rank
  variant
  parentBlockId
  parentPageId
}

query GetPage($id: ID!) {
  page(id: $id) {
    id
    emoji
    rawText
    richText
    favourite
    coverGradient
    blocks {
      ...PageBlock
    }
  }
}
