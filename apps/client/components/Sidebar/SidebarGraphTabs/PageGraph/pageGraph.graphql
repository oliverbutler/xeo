query GetPageGraph {
  pages(filters: { object: PAGE }) {
    id
    parentId
    properties {
      image {
        ... on Emoji {
          emoji
        }
      }
      title {
        rawText
      }
    }
  }
}
