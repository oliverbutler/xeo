overwrite: true
schema: 'http://localhost:3333/graphql'
generates:
  apps/client/generated.ts:
    documents: 'apps/client/**/*.tsx'
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    config:
      withHooks: true
